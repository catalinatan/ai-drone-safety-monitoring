safety:
  max_altitude: 50  # meters
  geofence_radius: 200  # meters from origin

navigation:
  speed: 5.0  # m/s for automatic mode
  position_tolerance: 2.0  # meters - how close to target before "arrived"
